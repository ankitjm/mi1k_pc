# Daily Notes — 2026-04-06

## KHO-207: RetailerOS Login Page Fix

### Session summary
Spent multiple heartbeats resolving KHO-207 (fix login page placeholder + demo credentials).

### Key lessons learned (hard way)

1. **Wrong server mistake**: Edited `/Users/ankitmehta/Projects/retaileros/` (local machine) and `khosha.tech` while the live site was on `187.77.12.140`. Board called it out — "do not use my local file".

2. **Netlify mistake**: Deploy agent used Netlify. Board was upset. Correct deployment: SSH to khosha.tech, edit source in `/var/www/retaileros/production/`, build, done.

3. **DNS discovery**: `retaileros.in` originally pointed to dev server `187.77.12.140`. Board changed DNS to `147.93.111.188` (khosha.tech) after I set up nginx config and requested it.

4. **Center column mistake**: Added `renderAuth()` to center column thinking it would fix login visibility. Board rejected — decorative "OS" text is intentional. Reverted.

5. **Empty database**: Demo retailer `ROS-20260225-0001` was never seeded into production PostgreSQL. That was the real reason for "Invalid store code". Fixed by INSERTing the retailer.

6. **nginx asset priority**: Static assets regex `~* \.(js|css)$` intercepted `/app/assets/` before the proxy location. Fixed with `location ^~ /app/` to give proxy location regex priority.

7. **Vite base path**: Set `base: '/app/'` so assets reference `/app/assets/` — avoids conflict with landing page assets at `/assets/`.

### Final state
- Login works: `ROS-20260225-0001` → JWT ✓
- Try Demo section visible in sidebar ✓
- Login form stays in sidebar (center column is decorative) ✓
- `retaileros.in/app/` → RetailerOS SPA ✓
- `retaileros.in/` → Landing page ✓
